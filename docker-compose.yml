services:
  ai-rabbi:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - CLAUDE_MODEL=${CLAUDE_MODEL:-claude-sonnet-4-20250514}
    volumes:
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
